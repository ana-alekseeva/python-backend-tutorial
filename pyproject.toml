[project]
name = "chatbot-api"
version = "0.1.0"
description = "Backbone chatbot backend: FastAPI + Nebius Token Factory"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.140.13",
    "openai>=2.49.0",
    "pydantic-settings>=2.14.2",
]

[tool.ruff]
line-length = 100
